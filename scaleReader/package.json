{
  "name": "@camstreamer/weightreader",
  "version": "1.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "clean": "rimraf dist/* html/* *.zip",
    "test": "echo \"Error: no test specified\" && exit 1",
    "in:ui": "cd react && yarn install",
    "in:prod": "npm install -production",
    "in:all": "npm install",
    "build:FE": "cd react && npm run build",
    "build": "npm-run-all build:FE",
    "start": "cd react && npm run dev",
    "zip:package": "node node_modules/camstreamerlib/CreatePackage -i -e=react,testing",
    "zip": "npm-run-all clean in:ui build:FE zip:package"
  },
  "author": "CamStreamer s.r.o",
  "license": "ISC",
  "dependencies": {
    "camstreamerlib": "^3.3.1",
    "rimraf": "^5.0.10"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
